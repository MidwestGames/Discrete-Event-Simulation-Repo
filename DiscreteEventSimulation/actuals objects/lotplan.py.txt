import numpy as np
import pandas as pd
from util import route as r
from util import table as t

class Lotplan:

    name : str

    routes : list[r.Route]

    def __init__(self, _lotplan : str):
        self.name = _lotplan

    def PopulateRoutes(self, tabs : list[t.Table], routes : list[r.Routes]):
        # TODO: Implement this function to populate routes using a list of tables and the global route list

        return -1

    def __to_string__(self):

        return "TODO: Create override __to_string__()"